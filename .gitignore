# General
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/
*.log
*.orig
*.rej

# WordPress / Build
node_modules/
# Root Composer dir only. A bare "vendor/" also matches nested paths such as
# modules/*/assets/vendor/, which would silently exclude a legitimately vendored
# runtime asset from both the repo and the release ZIP.
/vendor/
dist/
build/
*.zip

# Third-party archives dropped in for study — never commit (may be commercial).
*.rar
*.7z
mediacleanerpro711*

# Local config
wp-config.php

# Dev files
generate_rules.py
woobooster-rules-import.json
modules/woo-sheets-sync/proxy/

# Test artifacts
test_dir/
